using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Wall : MonoBehaviour
{
    //funciona como o Goal para detectar colisoes, mas ainda é usado ja que ele avisa as colisoes em paredes
    // para o boss, as paredes nao tem fisica, entao o boss nao sair delas é exclusivamente dado pelo seu aprendizado
    // ja que cada vez que ele encosta em uma parede ele é penalizado, ele decide por não fugir da area.
    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
